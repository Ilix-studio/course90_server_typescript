export enum PaymentStatus {
  CREATED = "created",
  PAID = "paid",
  FAILED = "failed",
  SUCCESS = "SUCCESS",
}

export enum PasskeyStatus {
  ACTIVE = "ACTIVE",
  EXPIRED = "EXPIRED",
  UNUSED = "UNUSED",
}

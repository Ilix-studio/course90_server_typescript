import asyncHandler from "express-async-handler";

const publishMockTest = asyncHandler(async () => {});

const getFeedMockTests = asyncHandler(async () => {});

export { publishMockTest, getFeedMockTests };
